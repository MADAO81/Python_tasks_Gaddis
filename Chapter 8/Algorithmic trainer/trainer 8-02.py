# Write a loop that counts the number of whitespace characters in a string
# value referenced by mystring.

counter = 0
for ch in mystring:
    if ch == " "
        counter +=1 
print("Number of whitespace characters:", counter)